services:
  claw-agent:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    working_dir: /workspace
    stdin_open: true
    tty: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - .:/workspace
